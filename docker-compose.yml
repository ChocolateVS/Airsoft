version: "3"

services:
  app:
    image: node:20
    restart: always
    container_name: Airsoft
    working_dir: /app
    volumes:
      - ./:/app # Map your local project directory to the container
    ports:
      - "40000:40000"
    command: npm run prod # The command to run your app
